apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':mtj-core')
    compile 'javax.annotation:jsr250-api:1.0'
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
